# Path to the COLMAP executable (e.g. colmap.bat on Windows)
COLMAP_PATH=C:/path/to/colmap/colmap.bat

# Path to the gaussian-splatting repo root
GS_PATH=C:/path/to/gaussian-splatting

# Directory where job data is stored (default: ./jobs)
JOBS_DIR=./jobs
